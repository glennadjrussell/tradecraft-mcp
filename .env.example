# Tradecraft MCP - API Keys (all optional)
# Tools that require missing keys will return helpful setup instructions.

SHODAN_API_KEY=          # shodan.io - host recon
CENSYS_API_ID=           # censys.io - host/cert search
CENSYS_API_SECRET=       # censys.io
VIRUSTOTAL_API_KEY=      # virustotal.com - malware/URL analysis
HIBP_API_KEY=            # haveibeenpwned.com - breach checking
ABUSEIPDB_API_KEY=       # abuseipdb.com - IP abuse reports
SECURITYTRAILS_API_KEY=  # securitytrails.com - subdomain discovery

# Authentication (optional — only applies to HTTP transports: sse, streamable-http)
MCP_AUTH_TOKEN=          # Bearer token required for all HTTP requests
MCP_AUTH_ISSUER_URL=     # OAuth issuer URL (default: http://localhost:<port>)
MCP_AUTH_SCOPES=         # Comma-separated required scopes (e.g. "read,write")
